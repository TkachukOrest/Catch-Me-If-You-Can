#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../karma-vs-reporter/karma-vs-reporter" "$@"
  ret=$?
else 
  node  "$basedir/../karma-vs-reporter/karma-vs-reporter" "$@"
  ret=$?
fi
exit $ret
